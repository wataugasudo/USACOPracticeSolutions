#Solves []

#Safely read list of String lines from input file
with open("FILENAME.in", "r") as inFile:
    text = inFile.readlines()

#Make a String line element of text[] into a list of integers
#variableName = list(map(int, text[0].split()))

#YOUR CODE HERE

#Safely (create &) write to output file
with open("FILENAME.out", "w") as outFile:
    outFile.write()
